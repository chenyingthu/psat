//=====================================================================
// 
//=====================================================================
#ifndef SOLVER_HEADER
#define SOLVER_HEADER
//---------------------------------------------------------------------
//---------------------------------------------------------------------
class Newton{
  double tol;
  int maxit;
  int debug;
  int method;
};
class Gmres{
  double tol_fixed;
  double tol;
  int maxit;
  int reorth;
  double *h;
  double *z;
  double *v;
  double *c;
  double *s;
};
class Precond{
  int inner;
  int outer;
  int innerStop;
  int outerStop;
};
class Jfng{
  Newton newton;
  Gmres gmres;
  Precond precond;

  double findiff;

};
class Update{};
class Solver{
  Jfng jfng;
  Update update;
};
#endif
//=====================================================================
